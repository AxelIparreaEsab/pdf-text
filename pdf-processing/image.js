// Extracción de imágenes desde PDF
export async function extractImagesFromPDF(pdfBuffer) {
  // Aquí va la lógica real (pdf-image, sharp, etc)
  // Por ahora solo retorna un array dummy
  return ['Imagen1 (dummy)', 'Imagen2 (dummy)'];
}
